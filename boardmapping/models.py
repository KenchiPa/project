from django.db import models


# Create your models here.

class BoardMapping(models.Model):
    # user = models.ForeignKey("users.User", on_delete=models.CASCADE, related_name='boardmapping')
    # board1_mapping = models.BooleanField(default=False)
    # board2_mapping = models.BooleanField(default=False)
    # board3_mapping = models.BooleanField(default=False)

    # class Meta:
    #     unique_together = (
    #         'user',
    #         'board1',
    #         'board2',
    #         'board3',
    #     )
    pass
    
    